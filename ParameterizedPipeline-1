pipeline{
    agent any
    parameters{
        string(name:'dev', defaultValue: 'tamil', description:'working on pipeline' )
    }
    stages{
        stage('Print the value of variables'){
            steps{
                echo "The dev name is ${params.dev}"
            }
        }
    }
}